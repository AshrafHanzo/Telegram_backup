// Mirrors app/src/App.css's --space-* and --radius-* scale.
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
} as const;

export const radii = {
  control: 12,
  container: 14,
  overlay: 20,
  pill: 999,
} as const;
